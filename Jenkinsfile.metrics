@Library(["ised-cicd-lib", "ised-cicd-lib-api"]) _

pipeline {
	agent {
       	label 'maven'
   	}
   	
    options {
        disableConcurrentBuilds()
    }
    
    stages {
    	stage('analyze') {
			steps {
				script{
					metrics.runCodeCoverage()
		    	}
			}
    	}
    }
}

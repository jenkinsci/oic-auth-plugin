jenkins:
  securityRealm:
    oic:
      clientId: clientId
      clientSecret: clientSecret
      serverConfiguration:
        wellKnown:
          wellKnownOpenIDConfigurationUrl: http://localhost:${MOCK_PORT}/well.known
